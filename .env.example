# ComfyUI Prometheus Exporter - example env file
# Copy to .env (same folder) and edit values.

# Metrics endpoint path
COMFY_PROM_METRICS_PATH=/metrics

# If ComfyUI is reachable at a different host/port than the /metrics request host/port,
# set this explicitly (otherwise exporter auto-detects from incoming request).
# COMFY_PROM_API_BASE=http://127.0.0.1:8188

# Optional API key header for ComfyUI endpoints
# COMFY_API_KEY=

# Cache TTLs
COMFY_PROM_CACHE_TTL_SECONDS=2.0
COMFY_PROM_SYSSTATS_TTL_SECONDS=10.0

# Logging: debug | info | warn | error
COMFY_PROM_LOG_LEVEL=info

# Optional: point to a different env file path
# COMFY_PROM_ENV_FILE=/path/to/comfyui_prometheus_exporter.env
